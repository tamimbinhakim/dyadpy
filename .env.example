# Copy to `.env` for local dev. Never commit `.env`.

# Dyadpy dev server
DYADPY_HOST=127.0.0.1
DYADPY_PORT=8000

# Where to write the generated TS client (relative to repo root)
DYADPY_CLIENT_OUT=examples/vite-react/frontend/src/lib/dyadpy/client.ts
